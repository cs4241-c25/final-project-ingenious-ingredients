export default function NotFound() {
    return (
        <main>
            <h1>Recipe not found</h1>
            <p>Unfortunately, we could not find the requested page or recipe.</p>
        </main>
    );
}